# Git commands
# configuring git user name and user email and confirming your confirmation
# probably connects the git account to computer git
git config --global user.name "Jane Doe" 
git config --global user.email janedoe@gmail.com 
git config --list 
# SSH/RSA Key/view public key
#ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIKxKoBlpEcxwLstfsRpyrTBGaKjdo8GXkrHPHEI/8ar3 mahmu@LAPTOP-M3F0PT14
# opening c drive in git bash. probably this also a command for command panel
pwd
# Linking existing project to Git in git bash
cd ~/dir/name/of/path/to/file
git init
git add . 
# Commit these changes to the git repository 
git commit -m "Initial commit" 
